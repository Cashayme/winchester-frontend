// Nuxt UI theme config inspiré du blason (noir/argent/rouge)
export default defineAppConfig({
  ui: {
    colors: {
      primary: 'red',   // alias pris en charge par Nuxt UI
      neutral: 'zinc'   // évite le fond bleu par défaut
    }
  }
});
